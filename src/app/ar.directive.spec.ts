import { ArDirective } from './ar.directive';

describe('ArDirective', () => {
  it('should create an instance', () => {
    const directive = new ArDirective();
    expect(directive).toBeTruthy();
  });
});
